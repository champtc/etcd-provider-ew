# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: ETCD Provider Build for DarkLight

on:
  push:
    branches: [ darklight ]
  pull_request:
    branches: [ darklight ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: GitHub Tag
      id: tag_version
      uses: mathieudutour/github-tag-action@v6.2
      with:
        github_token: ${{ secrets.GITHUB_TOKEN  }}

    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '11'
        cache: maven

    - name: maven-settings-xml-action
      uses: whelk-io/maven-settings-xml-action@v22
      with:
        repositories: '[{ "id": "central", "url": "https://repo1.maven.org/maven2" }, { "id": "etcd-provider-ew", "url": "https://maven.pkg.github.com/champtc/etcd-provider-ew" }]'
        servers: '[{ "id": "etcd-provider-ew", "username": "champtc", "password": "${{ secrets.GITHUB_TOKEN }}" }]'

    - name: Build with Maven
      run: mvn -B deploy -f ./pom.xml -Dtycho.localArtifacts=ignore -Dmaven.test.skip=true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}